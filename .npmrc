# react-simple-maps@3 declares peer deps on React 16/17/18 but works on React 19.
# Without this, Vercel's `npm install`/`npm ci` fails with ERESOLVE on the peer
# conflict. Matches how the dependency was installed locally.
legacy-peer-deps=true
